import React from "react";

function Tiles(props) {
    var numberOfTiles = props.children.length;

    return (
        <div className="tiles-container">

        </div>
    )
}

export default Tiles;